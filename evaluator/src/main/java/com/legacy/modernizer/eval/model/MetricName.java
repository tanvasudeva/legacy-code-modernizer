package com.legacy.modernizer.eval.model;

/** The four evaluation metrics produced by Phase 4.3. */
public enum MetricName {
    COMPILATION_SUCCESS,
    COVERAGE,
    API_COMPLETENESS,
    LLM_JUDGE_SCORE
}
